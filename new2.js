// let root2 = ReactDOM.createRoot(document.getElementById("two"))

// const p = <h2>hello react component</h2>
// //for rendering react component
// root2.render(p)

// const Heading = ()=>{
//     return(
//     <div>
//         {/*we can use parenthesis for using a react element inside fucntional component*/} 
//         {p}  
//          <h1>
//             heading 1

//         </h1>
//         <h2>
//            heading 2
//         </h2>
//     </div>
//     );
// };



// let root = ReactDOM.createRoot(document.getElementById("new"))

// // for rendering functional component
// root.render(<Heading/>)

/*
Create a Nested header Element using React.createElement(h1,h2,h3 inside a div with class “title”)
Create the same element using JSX
Create a functional component of the same with JSX
Pass attributes into the tag in JSX
Composition of Component(Add a component inside another)
{TitleComponent}
vs
{<TitleComponent/>}
vs
{<TitleComponent></TitleComponent>}
in JSX
Create a Header Component from scratch using Functional Components with JSX
Add a Logo on left
Add a search bar in middle
Add User icon on right
Add CSS to make it look nice



*/ 
// const Header = ()=>(
//     <div className="title">
//         <h1>h1</h1>
//         <h2>h2</h2>
//         <h3>h3</h3>
//     </div>
// );

//  let root = ReactDOM.createRoot(document.getElementById("new"))

// root.render(<Header/>)